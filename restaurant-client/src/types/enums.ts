export enum OrderStatus {
    Confirmed = 'Confirmed',
    InKitchen = 'InKitchen',
    Ready = 'Ready',
    Served = 'Served',
    Paid = 'Paid',
    Canceled = 'Canceled'
  }
  
  export enum Roles {
    Admin = 0,
    Manager = 1,
    Waiter = 2,
    Kitchen = 3
  }